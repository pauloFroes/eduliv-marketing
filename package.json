{
  "name": "eduliv-marketing",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "setup": "echo '🚀 Iniciando setup completo do projeto...' && npm install && echo '📄 Preparando arquivo de ambiente...' && cp env.example .env && echo '🐳 Subindo banco de dados...' && docker-compose up -d && echo '⏳ Aguardando banco inicializar (20s)...' && sleep 20 && echo '🔄 Gerando cliente Prisma...' && npx prisma generate && echo '🔍 Sincronizando schema do banco...' && npx prisma db push --accept-data-loss && echo '🌱 Executando seed...' && npx tsx prisma/seed.ts && echo '✅ Setup concluído! Iniciando servidor...' && npm run dev",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest --coverage",
    "db:up": "docker-compose up -d",
    "db:down": "docker-compose down",
    "db:reset": "docker-compose down -v && docker-compose up -d && sleep 20 && npx prisma generate && npx prisma db push --accept-data-loss && npx tsx prisma/seed.ts",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "db:seed": "tsx prisma/seed.ts",
    "seed": "npx tsx prisma/seed.ts",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:restart": "docker-compose restart",
    "docker:logs": "docker-compose logs -f postgres",
    "docker:clean": "docker-compose down -v --remove-orphans"
  },
  "dependencies": {
    "@prisma/client": "^6.10.1",
    "@types/jsonwebtoken": "^9.0.10",
    "bcrypt": "^6.0.0",
    "dotenv": "^16.5.0",
    "dotenv-expand": "^12.0.2",
    "jsonwebtoken": "^9.0.2",
    "next": "15.3.4",
    "prisma": "^6.10.1",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zod": "^3.25.67"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@tailwindcss/postcss": "^4",
    "@testing-library/jest-dom": "^6.6.3",
    "@types/bcrypt": "^5.0.2",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@vitest/ui": "^3.2.4",
    "eslint": "^9",
    "eslint-config-next": "15.3.4",
    "jsdom": "^26.1.0",
    "tailwindcss": "^4",
    "typescript": "^5",
    "vitest": "^3.2.4",
    "tsx": "^4.20.3"
  }
}
